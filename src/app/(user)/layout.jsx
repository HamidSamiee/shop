
import Header from "../Header";
import vazirFont from "../../constants/localFonts";
import "../globals.css";
import { Toaster } from "react-hot-toast";
import Providers from "../Providers";
import Footer from "../Footer";
import ScrollToTopButton from "@/components/ScrollToTopButton";
import { Suspense } from "react";




export const metadata = {
  title: "فروشگاه اینترنتی استار شاپ",
  description: "Generated by hsKala",
  icon: "/favicon.ico",
  
};
export const viewport={
  themeColor: '#60efff'
}

export default function RootLayout({ children }) {
  return (
    <html lang="fa" dir="rtl">
      <body className={`${vazirFont.variable} font-sans flex flex-col min-h-screen`} suppressHydrationWarning={true}>
        <Providers>
          <Toaster />
          <Suspense>
            <Header />
          </Suspense>
          
          <div className="container xl:max-w-screen-xl overflow-x-hidden flex-grow">
            {children}
            <ScrollToTopButton />  
          </div>
          <Footer />
        </Providers>
      </body>
    </html>
  );
}
